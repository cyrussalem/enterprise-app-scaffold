## IoT Platform PRD — Implementation Progress

### Task 1: Device Data Model — DONE (commit 9e356d4)
- Extended Device Sequelize model with 22 new fields:
  serial_number, manufacturer, model, device_type (enum), tags (array),
  last_seen_at, ip_address, signal_strength, latitude, longitude,
  location_label, timezone, firmware_version, hardware_revision,
  last_ota_update_at, battery_level, uptime_seconds, error_count,
  device_temperature, polling_interval_seconds, alert_threshold_config (jsonb), user_id
- Created TelemetryReading model (device_id, recorded_at, metric, value, unit)
  with composite index on (device_id, recorded_at)
- Created UserProfile model (user_id, whatsapp_number) for WhatsApp bot resolver
- Updated models/index.ts to initialise all three models
- Wrote 3 additive migrations for production:
  20260502000000-extend-devices-table.cjs
  20260502000001-create-telemetry-readings.cjs
  20260502000002-create-user-profiles.cjs
- All 26 unit tests pass; TypeScript build clean

### Task 2: Device Repository — DONE
- Created src/devices/device.repository.ts with all 8 typed methods:
  listDevices, getDevice, createDevice, updateDevice, deleteDevice,
  ingestTelemetry, queryTelemetry, getFleetSummary
- listDevices/getDevice/updateDevice/deleteDevice all filter by user_id (ownership enforcement)
- ingestTelemetry bulk-inserts readings and side-effects last_seen_at + status → online
- getFleetSummary runs two raw aggregation queries (by status, by device_type) and computes healthScore
- Added test/unit/device.repository.test.ts: 14 tests against real Postgres via docker-postgres helper
- All 40 unit tests pass; TypeScript build clean

### Task 3: Device CRUD HTTP endpoints — DONE
- Updated src/handlers/router.ts: added path-parameter matching so local dev server
  routes /v1/devices/abc → GET /v1/devices/{id} handler with pathParameters.id injected
- Created 7 new handlers:
  get-devices.ts (GET /v1/devices, with ?status and ?device_type filters)
  post-devices.ts (POST /v1/devices, sets user_id from auth token)
  get-device.ts (GET /v1/devices/{id})
  patch-device.ts (PATCH /v1/devices/{id})
  delete-device.ts (DELETE /v1/devices/{id})
  post-device-telemetry.ts (POST /v1/devices/{id}/telemetry, batch readings)
  get-dashboard-summary.ts (GET /v1/dashboard/summary)
- Updated src/handlers/api.ts to register all 7 new routes
- Updated infra/template.yaml with 7 new API Gateway event entries
- Added test/integration/devices.api.test.ts: 12 end-to-end tests
- All 40 unit tests pass; all 16 integration tests pass; TypeScript build clean

### Task 4: Seed script — DONE
- Created test/setup/seed-devices.js:
  - Logs in to Cognito to resolve demo user sub
  - Creates 50 devices: 10 per type (sensor/tracker/meter/actuator/gateway),
    cycled across 5 locations (London, New York, Tokyo, Sydney, Dubai)
  - Status distribution: 35 online / 8 offline / 7 warning (shuffled randomly)
  - Rich device fields: serial number, manufacturer, model, firmware version,
    lat/lon with jitter, IP, signal strength, battery level, uptime, tags,
    alert threshold config, last_seen_at (recency varies by status)
  - Generates 42 telemetry readings per metric per device (4-hour intervals, 7 days)
    with correlated random-walk values: 6,300 readings total
  - Bulk-inserts readings in batches of 1,000
- Updated package.json: integration:seed now chains seed-integration.js && seed-devices.js
- All 16 integration tests pass; TypeScript build clean

### Task 5: Fleet Overview — KPI cards + charts (issue 12) — DONE (commit e637aa1)
- Installed apexcharts + react-apexcharts in web/
- Created web/src/api/devices.ts: getDashboardSummary() and getDevices() typed wrappers
- Created web/src/pages/FleetOverviewPage.tsx at /fleet route:
  - KPI row: Total / Online / Offline / Warning MUI cards with colour-coded values
  - Status donut chart (ApexCharts): proportions of online/offline/warning
  - Device-type bar chart (ApexCharts): count per device type
  - Fleet health radial gauge (ApexCharts): single health-score percentage dial
  - Loading spinner and error alert states
- Updated AppShell: renamed to "IoT Platform", added Home + Fleet nav links, widened container to lg
- Updated App.tsx to register /fleet as an authenticated route
- Fixed seed-devices.js to use AccessToken sub (not IdToken sub) — cognito-local assigns
  different UUIDs per token type; the local server decodes Bearer (AccessToken), so the seeded
  user_id must match that sub
- All 16 integration tests pass; TypeScript build clean; Playwright verified at 50 devices

### Task 6: Fleet Overview — world map (issue 13) — DONE
- Added `import 'leaflet/dist/leaflet.css'` to web/src/main.tsx
- Created web/src/components/DeviceMap.tsx:
  - React Leaflet MapContainer with OpenStreetMap TileLayer (no API key)
  - Renders one CircleMarker per device that has lat/lon coordinates
  - Color-coded by status: green (#4caf50) = online, red (#f44336) = offline,
    amber (#ff9800) = warning
  - FitBounds child component calls map.fitBounds() on all pin coordinates on load
  - eventHandlers.click calls useNavigate(`/devices/${device.id}`) on each pin
  - Popup shows device name, location label, and status
- Updated web/src/pages/FleetOverviewPage.tsx:
  - Parallel fetch of getDashboardSummary + getDevices via Promise.all
  - Added devices state; DeviceMap rendered in a Card below the three charts
- All 16 integration tests pass; TypeScript + Vite build clean
- Playwright verified: 50 pins visible at 5 world locations, correct colors,
  pin click navigates to /devices/{id}

### Next: Task 7 — Per-device detail page (issues 15–17)
